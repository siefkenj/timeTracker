// template for an adjustable hour widget.  It has a starting hour and an ending hour
// that can be adjusted up and down.

div(class="adjustable-hour-container")
    | Start:
    input(class="start-hour", ng-model="startHour")
    button(ng-click="increment('start', '-')")="-"
    button(ng-click="increment('start', '+')")="+"
    | End: 
    input(class="start-hour", ng-model="endHour")
    button(ng-click="increment('end', '-')")="-"
    button(ng-click="increment('end', '+')")="+"
    span()=" ({{ endHour - startHour }} hours)"
